<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.millicent.benfromstupid">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".customview.CustomViewActivity" android:label="@string/custom_view"/>
        <activity android:name=".activity.CustomViewOneActivity" android:label="自定义view（一）"/>
        <activity android:name=".activity.PopupWindowsBottomActivity" android:label="底部弹窗PopupWindows"/>
    </application>

</manifest>